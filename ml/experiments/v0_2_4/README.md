# Филин v0.2.4 — baseline evaluation

Модель выбирается только по независимым train-runs. Test-runs используются один раз для итоговой внешней оценки и не участвуют в подборе признаков, preprocessing или гиперпараметров.
