# `receiver_batch_ack_v1`

ACK связывает пакет и попытку с долговечной фиксацией приёмника и содержит ровно один результат на событие. Неизвестный статус, частичное ACK, неверная контрольная сумма или `durable=false` отклоняются соединительным модулем.
