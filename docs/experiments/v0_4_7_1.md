# v0.4.7.1 — анализ отрицательного результата

Этап воспроизводимо разобрал отрицательный результат v0.4.7, не меняя его. Источниками служат только зафиксированные прогнозы, раскрытая после их фиксации разметка и существующие сводки. Контрольный набор `blind-pack:v047:01c5f40ba48a9944` больше не считается слепым.

## Результат

- разобраны 6 из 6 проваленных обязательных критериев;
- разобраны 6 из 6 критических различий;
- пересчитаны матрицы ошибок для 4104 оцениваемых окон;
- подтверждённых причин нет;
- 2 причины сильно обоснованы, 2 частично обоснованы, 1 остаётся гипотезой;
- подготовлено 7 корректирующих действий;
- проверка готовности имеет статус `ready` и разрешает только v0.4.7.2;
- v0.4.8 остаётся запрещённым.

Авторитетный результат: [`v0_4_7_1_policy_result.json`](../../ml/reports/v0_4_7_1/v0_4_7_1_policy_result.json).

## Ограничения

Раскрытый набор нельзя использовать для обучения, калибровки, выбора порогов, внутренней приёмки или новой слепой проверки. Анализ не является независимым подтверждением и не устанавливает внешнюю применимость.

## Проверка

```powershell
python -m tools.lab_console.verify_v0471
```
