# План технического лицензионного аудита

Последовательность: preflight и protected baseline; Git identities и file provenance; static assets; Python declarations/resolved environment; containers/system packages; REUSE и официальные тексты; profiles, notices, manifest и SBOM; positive/negative campaigns; регрессия проекта; один итоговый commit.

Любой `review_required` переводит результат в audit-only и `release_ready=false`.

