# Требования к публикации

До trial письменно определить право публиковать protocol, aggregate metrics,
limitations и отрицательный результат. Raw PCAP, IP, payload, labels,
credentials и сведения организации по умолчанию не публикуются. Scientific
claim должен явно описывать dataset scope и abstention coverage.
